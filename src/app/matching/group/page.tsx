/* Placeholder: the Nav already links here, so this route has to resolve to a
   real component. Replaced once group creation exists. */
export default function GroupMatchingPage() {
    return (
        <main style={{ padding: "4rem 1.5rem", textAlign: "center" }}>
            <h1>Groups</h1>
            <p>Group matching isn&apos;t built yet.</p>
        </main>
    );
}
